# Experiments

This folder contains experimental models that were evaluated during development.

## Angle Feature Experiment

- Added 10 finger joint angle features (73 total features)
- Trained a new SVM model
- Offline accuracy remained comparable to the baseline
- Real-time performance was less stable than the production model

The production application uses the models in the `/models` directory.